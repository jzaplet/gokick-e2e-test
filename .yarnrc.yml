nodeLinker: node-modules

# Project-local cache at .yarn/cache (instead of the user-global default).
# Makes the cache trivial to address from CI (actions/cache → .yarn/cache).
enableGlobalCache: false
